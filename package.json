{
  "name": "task-processing-engine",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:server": "turbo run dev --filter=server",
    "dev:client": "turbo run dev --filter=client",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.8.20"
  },
  "packageManager": "npm@10.9.2",
  "dependencies": {
    "@tanstack/react-query": "^5.95.2",
    "axios": "^1.13.6"
  }
}
